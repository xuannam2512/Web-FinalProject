function chooseActive(selectObject) {
    var id = selectObject.value;
    console.log(id);
    if(id == 'homeLink') {
        $('#home').addClass('table-active');
    }
    $('#home').addClass('table-active');
}